#include <stdio.h>

int main(){
    short M[2][2][2] = { {{10, 20}, {30, 40}},{{50, 60},{70, 80}} };
    int a, b, c;
    short cnt = 0;

    ++*M[0][0]; 
    *M[0][1] += 2;
    *M[1][0] += 3;
    *M[1][1] += 4;
    ++*(M[0][0]+1);
    *(M[0][1]+1) += 2;
    *(M[1][0]+1) += 3;
    *(*(M[0] + 0)) += 1;
    printf("%d\n", M[0]+0);
    printf("%d\n", *(M[0] + 0));
    printf("%d\n", *(*(M[0] + 0)));
    *(*(M[0] + 0) + 1) = *(*M[0] + 1) + *(**M + 1) + 2;
    *(*(M[0] + 1)) = *(*(*M + 1)) + 3;

    for(a = 0 ; a < 8 ; a++){
        if(M[0][0][a] >= 40)
            M[0][0][a] /= 2;
    }

    for(a = 0 ; a < 2 ; a++)
        for(b = 0 ; b < 2 ; b++)
            for(c = 0 ; c < 2 ; c++)
                if(M[a][b][c] / 10 == 2)
                    cnt++;
    printf("%d\n",cnt);
}

